{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=renovation --cleanup"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "moment": "^2.29.1",
    "momentjs": "^2.0.0",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.3",
    "push-dir": "^0.4.1"
  }
}
